local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

-- Lista com as IDs de usuário (UserIds) permitidas para rodar o Hub
local IDsAutorizadas = {
    123456789, -- 🌟 SUBSTUIA PELO SEU USERID DO ROBLOX
    987654321, -- ID de um amigo autorizado (opcional)
}

local acessoPermitido = false
for _, id in ipairs(IDsAutorizadas) do
    if LocalPlayer.UserId == id then
        acessoPermitido = true
        break
    end
end

if not acessoPermitido then
    local StarterGui = game:GetService("StarterGui")
    pcall(function()
        StarterGui:SetCore("SendNotification", {
            Title = "MagN Hub - Erro",
            Text = "Acesso Negado. Seu ID não está na Whitelist!",
            Duration = 10
        })
    end)
    return -- Cancela a execução do script imediatamente
end

local Libary = loadstring(game:HttpGet("https://pastefy.app/TYVuPNOS/raw"))()
workspace.FallenPartsDestroyHeight = -math.huge

local Window = Libary:MakeWindow({
    Title = "MagN Hub",
    SubTitle = "Para Brookhaven RP (Hub Destrutivo)",
    LoadText = "CHAOS ARQUIVO",
    Flags = "chaoshub_Broookhaven"
})
Window:AddMinimizeButton({
    Button = { Image = "rbxassetid://113213458632705", BackgroundTransparency = 0 },
    Corner = { CornerRadius = UDim.new(35, 1) },
})

local InfoTab = Window:MakeTab({ Title = "Info", Icon = "rbxassetid://10723415903" })

InfoTab:AddSection({ "Informações do Script" })
InfoTab:AddParagraph({ "Owner / Developer:", "Magnesiumv2" })
InfoTab:AddParagraph({ "Colaborações:", "ninguém" })
InfoTab:AddParagraph({ "Você está usando:", "MagN Hub" })

InfoTab:AddSection({ "Rejoin" })
InfoTab:AddButton({
    Name = "Rejoin",
    Callback = function()
        local TeleportService = game:GetService("TeleportService")
        TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, game.Players.LocalPlayer)
    end
})

local BossScript = Window:MakeTab({Title = "BruteScripts", Icon = "rbxassetid://114476297734336" })

BossScript:AddSection({"Destruição de Servidor"})
BossScript:AddButton({
   Name = "Ataque de Negação de Serviço",
   Description = "executa o Ataque de Sobrecarregamento do Servidor",
   Callback = function()
-- Evita múltiplos scripts rodando ao mesmo tempo e limpa variáveis
local player = game:GetService("Players").LocalPlayer
if not player then return end

local replicatedStorage = game:GetService("ReplicatedStorage")
local starterGui = game:GetService("StarterGui")

-- Função auxiliar para notificações
local function notify(title, text)
    pcall(function()
        starterGui:SetCore("SendNotification", {
            Title = title,
            Text = text,
            Button1 = "Ok",
            Duration = 5
        })
    end)
end

-- Salva a posição original de forma segura ANTES do loop
local character = player.Character or player.CharacterAdded:Wait()
local rootpart = character:WaitForChild("HumanoidRootPart", 10)
if not rootpart then 
    notify("Erro", "HumanoidRootPart não encontrado.")
    return 
end
local oldcf = rootpart.CFrame

-- Cache dos remotes para máxima velocidade
local re = replicatedStorage:FindFirstChild("RE")
local toolRemote = re and re:FindFirstChild("1Too1l")

if not toolRemote then 
    notify("Erro", "Remote funcional não encontrado.")
    return 
end

notify("Ataque de Negação de Serviço (DoS) Iniciado", "Aguarde os jogadores Crashar")

-- Thread separada para o loop de spam (Otimizado para não estourar a memória local)
task.spawn(function()
    for m = 1, 999999 do -- Ajustado para evitar estouro de buffer (Buffer Overflow) no cliente
        task.spawn(function()
            -- Nota: Se for um RemoteFunction, InvokeServer pode travar o script se o servidor lagar.
            if toolRemote:IsA("RemoteFunction") then
                toolRemote:InvokeServer("PickingTools", "FireHose")
            else
                toolRemote:FireServer("PickingTools", "FireHose")
            end
        end)

        local backpack = player:FindFirstChild("Backpack")
        if backpack then
            local fireHose = backpack:FindFirstChild("FireHose")
            if fireHose and fireHose:FindFirstChild("ToolSound") then
                fireHose.ToolSound:FireServer("FireHose", "DestroyFireHose")
            end
        end
        
        -- Controle de taxa de envio para o servidor processar o acúmulo
        if m % 15 == 0 then
            task.wait(0.1)
        end
    end
end)

-- Aguarda o acúmulo de requisições
task.wait(0.4)

-- Aguarda a destruição do corpo antigo e recarregamento completo do novo
player.CharacterRemoving:Wait()
local newCharacter = player.CharacterAdded:Wait()
local newRootPart = newCharacter:WaitForChild("HumanoidRootPart", 15)
local humanoid = newCharacter:WaitForChild("Humanoid", 15)

-- Retorna para a posição original guardada antes do reset com física estabilizada
if newRootPart and humanoid then
    task.wait(0.7) -- Janela de segurança ideal para o Anticheat registrar a nova ID do personagem
    
    -- Desativa temporariamente estados de queda para evitar detecção de velocidade
    humanoid:ChangeState(Enum.HumanoidStateType.Physics)
    newRootPart.CFrame = oldcf
    
    -- Restaura velocidade linear zerada para evitar borracha (rubberband)
    newRootPart.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
end

notify("Script de Dupe", "Concluído, Equipando Itens...")
task.wait(0.5)

-- Equipar todas as FireHoses clonadas da Backpack com proteção de travamento
local backpack = player:WaitForChild("Backpack", 10)
if backpack then
    local items = backpack:GetChildren()
    local equipCount = 0
    
    for i = 1, #items do
        local item = items[i]
        if item:IsA("Tool") and item.Name == "FireHose" then
            equipCount = equipCount + 1
            task.defer(function()
                item.Parent = newCharacter
            end)
            
            -- Evita que a engine ignore ferramentas por excesso de troca de parent simultânea
            if equipCount % 8 == 0 then 
                task.wait(0.02) 
            end 
        end
    end
end

task.wait(0.5)
notify("Script de Dupe", "Finalizando processo...")
task.wait(0.2)
end
})

BossScript:AddButton({
   Name = "Bring Parts Agressive",
   Description= "Traz todas as Parts não ancoradas para o player escrito na textbox",
   Callback = function()
-- Gui to Lua - VERSÃO AGRESSIVA
-- Versão Otimizada com Velocidade Aumentada

local Gui = Instance.new("ScreenGui")
local Main = Instance.new("Frame")
local Box = Instance.new("TextBox")
local UITextSizeConstraint = Instance.new("UITextSizeConstraint")
local Label = Instance.new("TextLabel")
local UITextSizeConstraint_2 = Instance.new("UITextSizeConstraint")
local Button = Instance.new("TextButton")
local UITextSizeConstraint_3 = Instance.new("UITextSizeConstraint")
local SpeedSlider = Instance.new("Frame")
local SpeedLabel = Instance.new("TextLabel")
local SpeedValue = Instance.new("TextLabel")

-- Properties
Gui.Name = "Gui"
Gui.Parent = gethui()
Gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

Main.Name = "Main"
Main.Parent = Gui
Main.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Main.BorderColor3 = Color3.fromRGB(255, 0, 0)
Main.BorderSizePixel = 3
Main.Position = UDim2.new(0.335954279, 0, 0.542361975, 0)
Main.Size = UDim2.new(0.270350261, 0, 0.220880623, 0)
Main.Active = true
Main.Draggable = true

Box.Name = "Box"
Box.Parent = Main
Box.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
Box.BorderColor3 = Color3.fromRGB(255, 0, 0)
Box.BorderSizePixel = 2
Box.Position = UDim2.new(0.0980926454, 0, 0.15, 0)
Box.Size = UDim2.new(0.801089942, 0, 0.28, 0)
Box.FontFace = Font.new("rbxasset://fonts/families/SourceSansSemibold.json", Enum.FontWeight.Bold, Enum.FontStyle.Normal)
Box.PlaceholderText = "Player aqui"
Box.Text = ""
Box.TextColor3 = Color3.fromRGB(255, 50, 50)
Box.TextScaled = true
Box.TextSize = 31.000
Box.TextWrapped = true

UITextSizeConstraint.Parent = Box
UITextSizeConstraint.MaxTextSize = 31

Label.Name = "Label"
Label.Parent = Main
Label.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
Label.BorderColor3 = Color3.fromRGB(255, 0, 0)
Label.BorderSizePixel = 2
Label.Size = UDim2.new(1, 0, 0.12, 0)
Label.FontFace = Font.new("rbxasset://fonts/families/Nunito.json", Enum.FontWeight.Bold, Enum.FontStyle.Normal)
Label.Text = "AGGRESSIVE BRING PARTS v2 🔥"
Label.TextColor3 = Color3.fromRGB(255, 50, 50)
Label.TextScaled = true
Label.TextSize = 14.000
Label.TextWrapped = true

UITextSizeConstraint_2.Parent = Label
UITextSizeConstraint_2.MaxTextSize = 21

Button.Name = "Button"
Button.Parent = Main
Button.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
Button.BorderColor3 = Color3.fromRGB(255, 255, 255)
Button.BorderSizePixel = 2
Button.Position = UDim2.new(0.1, 0, 0.55, 0)
Button.Size = UDim2.new(0.8, 0, 0.25, 0)
Button.Font = Enum.Font.Nunito
Button.Text = "INICIAR | OFF"
Button.TextColor3 = Color3.fromRGB(255, 255, 255)
Button.TextScaled = true
Button.TextSize = 28.000
Button.TextWrapped = true

UITextSizeConstraint_3.Parent = Button
UITextSizeConstraint_3.MaxTextSize = 28

-- Scripts
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")

local character
local humanoidRootPart
local mainStatus = true
local aggressiveSpeed = 500 -- Velocidade aumentada
local responsiveness = 500 -- Responsividade alta

UserInputService.InputBegan:Connect(function(input, gameProcessedEvent)
    if input.KeyCode == Enum.KeyCode.RightControl and not gameProcessedEvent then
        mainStatus = not mainStatus
        Main.Visible = mainStatus
    end
end)

local Folder = Instance.new("Folder", Workspace)
local Part = Instance.new("Part", Folder)
local Attachment1 = Instance.new("Attachment", Part)
Part.Anchored = true
Part.CanCollide = false
Part.Transparency = 1

if not getgenv().NetworkAggressive then
    getgenv().NetworkAggressive = {
        BaseParts = {},
        Velocity = Vector3.new(50, 50, 50)
    }

    NetworkAggressive.RetainPart = function(Part)
        if Part:IsA("BasePart") and Part:IsDescendantOf(Workspace) then
            table.insert(NetworkAggressive.BaseParts, Part)
            Part.CustomPhysicalProperties = PhysicalProperties.new(0, 0, 0, 0, 0)
            Part.CanCollide = false
        end
    end

    local function EnablePartControl()
        LocalPlayer.ReplicationFocus = Workspace
        RunService.Heartbeat:Connect(function()
            sethiddenproperty(LocalPlayer, "SimulationRadius", math.huge)
            for _, Part in pairs(NetworkAggressive.BaseParts) do
                if Part:IsDescendantOf(Workspace) then
                    Part.Velocity = NetworkAggressive.Velocity
                end
            end
        end)
    end

    EnablePartControl()
end

local function ForcePart(v)
    if v:IsA("BasePart") and not v.Anchored and not v.Parent:FindFirstChildOfClass("Humanoid") and not v.Parent:FindFirstChild("Head") and v.Name ~= "Handle" then
        for _, x in ipairs(v:GetChildren()) do
            if x:IsA("BodyMover") or x:IsA("RocketPropulsion") or x:IsA("Torque") or x:IsA("AlignPosition") then
                x:Destroy()
            end
        end
        if v:FindFirstChild("Attachment") then
            v:FindFirstChild("Attachment"):Destroy()
        end
        if v:FindFirstChild("AlignPosition") then
            v:FindFirstChild("AlignPosition"):Destroy()
        end
        if v:FindFirstChild("Torque") then
            v:FindFirstChild("Torque"):Destroy()
        end
        v.CanCollide = false
        
        local Torque = Instance.new("Torque", v)
        Torque.Torque = Vector3.new(500000, 500000, 500000) -- Torque aumentado
        local AlignPosition = Instance.new("AlignPosition", v)
        local Attachment2 = Instance.new("Attachment", v)
        Torque.Attachment0 = Attachment2
        AlignPosition.MaxForce = math.huge
        AlignPosition.MaxVelocity = math.huge
        AlignPosition.Responsiveness = responsiveness -- Responsiveness aumentada
        AlignPosition.Attachment0 = Attachment2
        AlignPosition.Attachment1 = Attachment1
    end
end

local blackHoleActive = false
local DescendantAddedConnection
local updateConnection

local function toggleBlackHole()
    blackHoleActive = not blackHoleActive
    if blackHoleActive then
        Button.Text = "PARANDO | ON 🔥"
        Button.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
        
        -- Força TODAS as partes do mapa
        for _, v in ipairs(Workspace:GetDescendants()) do
            ForcePart(v)
        end

        DescendantAddedConnection = Workspace.DescendantAdded:Connect(function(v)
            if blackHoleActive then
                task.spawn(function()
                    ForcePart(v)
                end)
            end
        end)

        -- Atualização super rápida
        updateConnection = RunService.RenderStepped:Connect(function()
            if blackHoleActive and humanoidRootPart then
                Attachment1.WorldCFrame = humanoidRootPart.CFrame
                -- Boost extra de velocidade
                NetworkAggressive.Velocity = Vector3.new(aggressiveSpeed, aggressiveSpeed, aggressiveSpeed)
            end
        end)
    else
        Button.Text = "INICIAR | OFF"
        Button.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        if DescendantAddedConnection then
            DescendantAddedConnection:Disconnect()
        end
        if updateConnection then
            updateConnection:Disconnect()
        end
    end
end

local function getPlayer(name)
    local lowerName = string.lower(name)
    for _, p in pairs(Players:GetPlayers()) do
        local lowerPlayer = string.lower(p.Name)
        if string.find(lowerPlayer, lowerName) then
            return p
        elseif string.find(string.lower(p.DisplayName), lowerName) then
            return p
        end
    end
end

local player = nil

local function setupBoxScript()
    local script = Instance.new('Script', Box)
    script.Parent.FocusLost:Connect(function(enterPressed)
        if enterPressed then
            player = getPlayer(Box.Text)
            if player then
                Box.Text = player.Name
                print("✅ Player encontrado:", player.Name)
            else
                print("❌ Player não encontrado")
            end
        end
    end)
end
setupBoxScript()

local function setupButtonScript()
    local script = Instance.new('Script', Button)
    script.Parent.MouseButton1Click:Connect(function()
        if player then
            character = player.Character or player.CharacterAdded:Wait()
            humanoidRootPart = character:WaitForChild("HumanoidRootPart")
            toggleBlackHole()
        else
            print("❌ Player não foi selecionado!")
        end
    end)
end
setupButtonScript()
end
})

local BombActive = false

BossScript:AddToggle({
    Name = "Lag com Bomba",
    Default = false,
    Callback = function(Value)
        if Value then
            BombActive = true

            local Player = game.Players.LocalPlayer
            local Character = Player.Character or Player.CharacterAdded:Wait()
            local RootPart = Character:WaitForChild("HumanoidRootPart")
            local WorkspaceService = game:GetService("Workspace")
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            local Bomb = WorkspaceService:WaitForChild("WorkspaceCom"):WaitForChild("001_CriminalWeapons"):WaitForChild("GiveTools"):WaitForChild("Bomb")

            task.spawn(function()
                while BombActive do
                    if Bomb and RootPart then
                        RootPart.CFrame = Bomb.CFrame
                        fireclickdetector(Bomb.ClickDetector) -- Aciona o ClickDetector da bomba
                        task.wait(0.1) -- Delay mínimo para evitar travamentos
                    else
                        task.wait(0.1)
                    end
                end
            end)

            task.spawn(function()
                while BombActive do
                    if Bomb and RootPart then
                        local VirtualInputManager = game:GetService("VirtualInputManager")
                        VirtualInputManager:SendMouseButtonEvent(500, 500, 0, true, game, 0) 
                        task.wait(1.5)
                        VirtualInputManager:SendMouseButtonEvent(500, 500, 0, false, game, 0) 

                        -- Executa o FireServer com o nome do jogador
                        local args = {
                            [1] = "Bomb" .. Player.Name -- Usa o nome do jogador atual
                        }
                        ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Blo1wBomb1sServe1r"):FireServer(unpack(args))
                    end
                    task.wait(1.5) -- Intervalo de 1 segundo para clique e FireServer
                end
            end)
        else
            -- Desativando a funcionalidade
            BombActive = false
        end
    end
})

-- VERSÃO ADAPTADA PARA BOTÃO (BUTTON)
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local StarterGui = game:GetService("StarterGui")

local LocalPlayer = Players.LocalPlayer

BossScript:AddButton({
    Name = "Kick All/ShutDown Server/Ataque de Solicitação",
    Callback = function()
        local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
        local rootpart = character:WaitForChild("HumanoidRootPart")
        local backpack = LocalPlayer:WaitForChild("Backpack")
        local remoteStorage = ReplicatedStorage:WaitForChild("RE")
        
        -- 1. Loop de requisição de ferramentas
        for m = 1, 535 do
            local args1 = {
                [1] = "PickingTools",
                [2] = "FireHose"
            }
            local toolRemote = remoteStorage:FindFirstChild("1Too1l")
            if toolRemote and toolRemote:IsA("RemoteFunction") then
                toolRemote:InvokeServer(unpack(args1))
            end
            
            -- Proteção para evitar travamentos caso ToolSound não seja um RemoteEvent válido
            local fireHose = backpack:FindFirstChild("FireHose")
            if fireHose and fireHose:FindFirstChild("ToolSound") then
                local args2 = {
                    [1] = "FireHose",
                    [2] = "DestroyFireHose"
                }
                pcall(function()
                    fireHose.ToolSound:FireServer(unpack(args2))
                end)
            end
        end
        
        -- 2. Salvar posição antiga e teleportar para o Void
        local oldcf = rootpart.CFrame
        rootpart.CFrame = CFrame.new(999999999.414, -475, 999999999.414)
        
        -- Espera otimizada usando task.wait()
        repeat task.wait() until rootpart.Parent == nil
        repeat task.wait() until character:FindFirstChild("HumanoidRootPart")
        
        -- Retornar à posição antiga
        rootpart.CFrame = oldcf
        
        StarterGui:SetCore("SendNotification", {
            Title = "Script de Dupe",
            Text = "Shutdown Concluído, Agora Vai Desligar",
            Button1 = "Ok",
            Duration = 5
        })
        
        task.wait(0.1)
        
        -- 3. Equipar todas as FireHose da Backpack para o Personagem
        for _, item in ipairs(backpack:GetChildren()) do
            if item.Name == "FireHose" and item:IsA("Tool") then
                item.Parent = character
            end
        end
        
        task.wait(0.2)
        
        StarterGui:SetCore("SendNotification", {
            Title = "Script de Dupe",
            Text = "Iniciando duplicação, seja paciente",
            Button1 = "Ok",
            Duration = 5
        })
        
        task.wait(0.5)
        
        -- Teleporte final de processamento
        if character:FindFirstChild("HumanoidRootPart") then
            character.HumanoidRootPart.CFrame = CFrame.new(9999, -475, 9999)
        end
    end
})

local Players = game:GetService("Players")
local targetName = "" -- Variável ÚNICA que todas as funções vão ler

-- Função global para puxar e atualizar os nomes dos jogadores de forma limpa
local function getPlayerNames()
    local names = {}
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= Players.LocalPlayer then
            table.insert(names, player.Name)
        end
    end
    return names
end

-- ====================================================================
-- DROPDOWN UNIFICADO (Coloque no início da sua aba de Moderação)
-- ====================================================================
local MasterDropdown = BossScript:AddDropdown({
    Name = "🎯 SELECIONAR ALVO GERAL",
    Options = getPlayerNames(),
    Default = "Nenhum",
    Callback = function(selectedOption)
        targetName = selectedOption
        print("[MagN Hub]: Alvo definido para todas as funções: " .. targetName)
    end
})

-- Botão essencial para atualizar o Dropdown se alguém entrar ou sair
BossScript:AddButton({
    Name = "🔄 Atualizar Lista de Alvos",
    Callback = function()
        MasterDropdown:Refresh(getPlayerNames(), true)
    end
})

-- ====================================================================
-- CORREÇÃO: ATAQUE DE ABAJUR (TOGGLE)
-- ====================================================================
local lampActive = false
BossScript:AddToggle({
    Name = "Ativar Ataque de Abajur",
    Default = false,
    Callback = function(state)
        lampActive = state
        
        if lampActive then
            local target = Players:FindFirstChild(targetName)
            if not target or not target.Character then
                print("[MagN Hub]: Nenhum alvo válido selecionado no Dropdown Geral.")
                lampActive = false
                return
            end
            
            task.spawn(function()
                while lampActive and target.Parent and target.Character do
                    local root = target.Character:FindFirstChild("HumanoidRootPart")
                    if root then
                        -- Posição colada no alvo
                        local spawnPos = root.CFrame * CFrame.new(0, 0, -1)
                        
                        -- Loop forçado de 15 unidades
                        for i = 1, 15 do
                            -- CORREÇÃO DO REMOTE: Certifique-se de que o caminho abaixo bate com o seu Remote Spy!
                            -- O Brookhaven geralmente exige os argumentos: (ID_Do_Prop, CFrame_Posicao)
                            pcall(function()
                                -- Exemplo técnico corrigido:
                                -- game:GetService("ReplicatedStorage").CaminhoDoRemoteDeProps:FireServer("LampID", spawnPos)
                            end)
                        end
                    end
                    task.wait(0.4) -- Tempo seguro para atualizar a perseguição se o alvo correr
                end
            end)
        end
    end
})

-- ====================================================================
-- SEUS OUTROS BOTÕES (Exemplo: Crash Cliente)
-- ====================================================================
BossScript:AddButton({
    Name = "💥 Executar Instant Client Crash",
    Callback = function()
        local target = Players:FindFirstChild(targetName) -- Lendo a variável unificada
        if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
            local targetCFrame = target.Character.HumanoidRootPart.CFrame
            
            print("[MagN Hub]: Crashando " .. target.Name)
            for i = 1, 800 do
                pcall(function()
                    -- Seu remote de partículas/efeitos pesados do Brookhaven aqui
                    -- game:GetService("ReplicatedStorage").CaminhoDoRemoteDeEfeitos:FireServer("FumaçaMotorID", targetCFrame)
                end)
            end
        else
            print("[MagN Hub]: Selecione um alvo no Dropdown Geral primeiro!")
        end
    end
})

-- ====================================================================
-- CORREÇÃO DEFINITIVA: COUCH FREEZE (TOGGLE)
-- ====================================================================
local couchFreezeActive = false
local freezeLocation = nil

BossScript:AddToggle({
    Name = "Ativar Couch Freeze (Travar Alvo)",
    Default = false,
    Callback = function(state)
        couchFreezeActive = state
        
        if couchFreezeActive then
            local target = Players:FindFirstChild(targetName) -- Usa a variável mestre do seu dropdown
            local localPlayer = Players.LocalPlayer
            
            -- Validações antes de iniciar o travamento
            if not target or target == localPlayer then
                print("[MagN Hub]: Selecione um alvo válido no Dropdown Geral primeiro!")
                couchFreezeActive = false
                return
            end
            
            if not localPlayer.Character or not localPlayer.Character:FindFirstChild("HumanoidRootPart") then
                print("[MagN Hub]: Seu personagem não carregou completamente.")
                couchFreezeActive = false
                return
            end
            
            -- Salva a localização exata de onde VOCÊ ativou o comando
            freezeLocation = localPlayer.Character.HumanoidRootPart.CFrame
            print("[MagN Hub]: Coordenada de prisão salva! Iniciando trava de física...")
            
            -- Loop de alta velocidade em segundo plano para vencer qualquer tentativa de fuga
            task.spawn(function()
                while couchFreezeActive and target.Parent and target.Character do
                    local humanoid = target.Character:FindFirstChildOfClass("Humanoid")
                    
                    if humanoid then
                        -- Passo 1: Se o mal-intencionado NÃO estiver sentado, força o spawn do sofá nele
                        if humanoid.SeatPart == nil then
                            pcall(function()
                                -- Substitua pelo caminho e ID do sofá real mapeado no seu Remote Spy
                                -- Argumentos típicos do Brookhaven: (ID_Do_Sofá, CFrame_De_Spawn)
                                -- game:GetService("ReplicatedStorage").CaminhoDoRemote:FireServer("SofaID", target.Character.HumanoidRootPart.CFrame)
                            end)
                        end
                        
                        -- Passo 2: Se ele já estiver preso no assento, puxa a peça para a prisão fixa
                        if humanoid.SeatPart ~= nil and humanoid.SeatPart.Name == "Seat" then
                            pcall(function()
                                local seatPart = humanoid.SeatPart
                                
                                -- Força a propriedade de rede e coordenada da peça localmente
                                seatPart.CFrame = freezeLocation
                                seatPart.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                                seatPart.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
                                
                                -- Se o assento fizer parte de um modelo de prop, move o modelo inteiro junto
                                local model = seatPart.Parent
                                if model and model:IsA("Model") then
                                    model:MoveTo(freezeLocation.Position)
                                end
                            end)
                        end
                    end
                    
                    task.wait(0.05) -- Loop ultra rápido (20 vezes por segundo) para impedir o movimento
                end
                
                print("[MagN Hub]: Couch Freeze finalizado ou alvo desconectado.")
            end)
            
        else
            print("[MagN Hub]: Couch Freeze desativado manualmente.")
            -- Executa a limpeza automática de props para sumir com o sofá do mapa assim que você desliga
            pcall(function()
                -- game:GetService("ReplicatedStorage").CaminhoDoRemoteDeLimpar:FireServer()
            end)
        end
    end
})

local seatSpamActive = false

ModTab:AddToggle({
    Name = "Ativar Spam de Cadeira (Atrapalhar Alvo)",
    Default = false,
    Callback = function(state)
        seatSpamActive = state
        
        if seatSpamActive then
            local target = game.Players:FindFirstChild(targetName) -- Lê o alvo do seu Dropdown Geral
            if not target or target == game.Players.LocalPlayer then 
                print("[MagN Hub]: Nenhum alvo válido selecionado.")
                seatSpamActive = false
                return 
            end
            
            print("[MagN Hub]: Iniciando Spam de Cadeira contra " .. target.Name)
            
            -- Loop rápido em segundo plano
            task.spawn(function()
                while seatSpamActive and target.Parent and target.Character do
                    local root = target.Character:FindFirstChild("HumanoidRootPart")
                    local humanoid = target.Character:FindFirstChildOfClass("Humanoid")
                    
                    if root and humanoid then
                        -- Se ele NÃO estiver sentado, spawna a cadeira nas pernas dele
                        if humanoid.SeatPart == nil then
                            pcall(function()
                                -- Posição ligeiramente abaixo do tronco para forçar o assento instantâneo
                                local cadeiraPos = root.CFrame * CFrame.new(0, -1, 0)
                                
                                -- 🔴 NOTA: Substitua o comando abaixo pelo Remote real de Cadeiras do Brookhaven
                                -- game:GetService("ReplicatedStorage").CaminhoDoRemote:FireServer("CadeiraID", cadeiraPos)
                            end)
                        else
                            -- Se ele já sentou, força ele a levantar para reiniciar o ciclo e travar a andada
                            pcall(function()
                                humanoid.Sit = false 
                                -- Opcional: Chama o remote de limpar props para sumir com a cadeira antiga
                                -- game:GetService("ReplicatedStorage").CaminhoDoRemoteDeLimpar:FireServer()
                            end)
                        end
                    end
                    task.wait(0.2) -- Intervalo para travar a movimentação sem pesar o jogo
                end
            end)
        else
            print("[MagN Hub]: Spam de Cadeira desativado. Limpando props...")
            pcall(function()
                -- game:GetService("ReplicatedStorage").CaminhoDoRemoteDeLimpar:FireServer()
            end)
        end
    end
})

local Troll = Window:MakeTab({ Title = "Kill Player", Icon = "rbxassetid://11415789819" })
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local VirtualInputManager = game:GetService("VirtualInputManager")
local RunService = game:GetService("RunService")
local cam = workspace.CurrentCamera


local selectedPlayerName = nil
local methodKill = nil
getgenv().Target = nil
local Character = LocalPlayer.Character
local Humanoid = Character and Character:WaitForChild("Humanoid")
local RootPart = Character and Character:WaitForChild("HumanoidRootPart")

local function getPlayerList()
        local players = Players:GetPlayers()
        local playerNames = {}
        for _, player in ipairs(players) do
            if player ~= LocalPlayer then
                table.insert(playerNames, player.Name)
            end
        end
        return playerNames
    end

    local killDropdown = Troll:AddDropdown({
        Name = "Selecionar Jogador",
        Options = getPlayerList(),
        Default = "",
        Callback = function(value)
            selectedPlayerName = value
            getgenv().Target = value
            print("Jogador selecionado: " .. tostring(value))
        end
    })

    Troll:AddButton({
        Name = "Atualizar Player List",
        Callback = function()
            local tablePlayers = Players:GetPlayers()
            local newPlayers = {}
            if killDropdown and #tablePlayers > 0 then
                for _, player in ipairs(tablePlayers) do
                    if player.Name ~= LocalPlayer.Name then
                        table.insert(newPlayers, player.Name)
                    end
                end
                killDropdown:Set(newPlayers)
                print("Lista de jogadores atualizada: ", table.concat(newPlayers, ", "))
                if selectedPlayerName and not Players:FindFirstChild(selectedPlayerName) then
                    selectedPlayerName = nil
                    getgenv().Target = nil
                    killDropdown:SetValue("")
                    print("Seleção resetada, jogador não está mais no servidor.")
                end
            else
                print("Erro: Dropdown não encontrado ou nenhum jogador disponível.")
            end
        end
    })

Troll:AddToggle({
        Name = "Spectar Player",
        Default = false,
        Callback = function(value)
            local Camera = workspace.CurrentCamera

            local function UpdateCamera()
                if value then
                    local targetPlayer = Players:FindFirstChild(selectedPlayerName)
                    if targetPlayer and targetPlayer.Character then
                        local humanoid = targetPlayer.Character:FindFirstChild("Humanoid")
                        if humanoid then
                            Camera.CameraSubject = humanoid
                        end
                    end
                else
                    if LocalPlayer.Character then
                        local humanoid = LocalPlayer.Character:FindFirstChild("Humanoid")
                        if humanoid then
                            Camera.CameraSubject = humanoid
                        end
                    end
                end
            end

            if value then
                if not getgenv().CameraConnection then
                    getgenv().CameraConnection = RunService.Heartbeat:Connect(UpdateCamera)
                end
            else
                if getgenv().CameraConnection then
                    getgenv().CameraConnection:Disconnect()
                    getgenv().CameraConnection = nil
                end
                UpdateCamera()
            end
        end
    })

Troll:AddToggle({
    Name = "Fling Couch",
    Default = false,
    Callback = function(state)

        flingActive = state
        if state and selectedPlayerName then
            local target = Players:FindFirstChild(selectedPlayerName)
            if not target or not target.Character then return end
            local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
            local tRoot = target.Character and target.Character:FindFirstChild("Head")
            if not root or not tRoot then return end
            local char = LocalPlayer.Character
            local hum = char:FindFirstChildOfClass("Humanoid")
            local original = root.CFrame

local args = {
	"ClearAllTools"
}
game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer(unpack(args))

task.wait(0.0001)


            local args = {
                [1] = "PickingTools",
                [2] = "Couch"
            }
            game:GetService("ReplicatedStorage").RE:FindFirstChild("1Too1l"):InvokeServer(unpack(args))

            task.wait(0.0001)
            local tool = LocalPlayer.Backpack:FindFirstChild("Couch")
            if tool then
                tool.Parent = char
            end
				task.wait(0.0001)

            workspace.FallenPartsDestroyHeight = 0/0
            local bv = Instance.new("BodyVelocity")
            bv.Name = "FlingForce"
            bv.Velocity = Vector3.new(9e8, 9e8, 9e8)
            bv.MaxForce = Vector3.new(1/0, 1/0, 1/0)
            bv.Parent = root
            hum:SetStateEnabled(Enum.HumanoidStateType.Seated, false)
            hum.PlatformStand = false
            cam.CameraSubject = target.Character:FindFirstChild("Head") or tRoot or hum

wait(0.001)

game:GetService("VirtualInputManager"):SendKeyEvent(true, Enum.KeyCode.F, false, game)

            task.spawn(function()
                local angle = 0
                local parts = {root}
                while flingActive and target and target.Character and target.Character:FindFirstChildOfClass("Humanoid") do
                    local tHum = target.Character:FindFirstChildOfClass("Humanoid")
                    if tHum.Sit then break end
                    angle += 50

                    for _, part in ipairs(parts) do
                        local pos_x = target.Character.Head.Position.X
                        local pos_y = target.Character.Head.Position.Y
                        local pos_z = target.Character.Head.Position.Z
                        pos_x = pos_x + (target.Character.Head.Velocity.X / 1.5)
                        pos_y = pos_y + (target.Character.Head.Velocity.Y / 1.5)
                        pos_z = pos_z + (target.Character.Head.Velocity.Z / 1.5)
                        root.CFrame = CFrame.new(pos_x, pos_y, pos_z) * CFrame.Angles(math.rad(angle), 0, 0)
                    end

                    root.Velocity = Vector3.new(9e8, 9e8, 9e8)
                    root.RotVelocity = Vector3.new(9e8, 9e8, 9e8)
                    task.wait()
                end
                flingActive = false
                bv:Destroy()
                hum:SetStateEnabled(Enum.HumanoidStateType.Seated, false)
                hum.PlatformStand = false
                root.CFrame = original
                cam.CameraSubject = hum
                for _, p in pairs(char:GetDescendants()) do
                    if p:IsA("BasePart") then
                        p.Velocity = Vector3.zero
                        p.RotVelocity = Vector3.zero
                    end
                end
                hum:UnequipTools()
                game:GetService("ReplicatedStorage").RE:FindFirstChild("1Too1l"):InvokeServer(unpack(args))
            end)
        end
    end
})

local function FlingBall(target)

    local players = game:GetService("Players")
    local player = players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild("Humanoid")
    local hrp = character:WaitForChild("HumanoidRootPart")
    local backpack = player:WaitForChild("Backpack")
    local ServerBalls = workspace.WorkspaceCom:WaitForChild("001_SoccerBalls")

    local function GetBall()
        if not backpack:FindFirstChild("SoccerBall") then
            game:GetService("ReplicatedStorage").RE:FindFirstChild("1Too1l"):InvokeServer("PickingTools", "SoccerBall")
        end
        repeat task.wait() until backpack:FindFirstChild("SoccerBall")
        backpack.SoccerBall.Parent = character
        repeat task.wait() until ServerBalls:FindFirstChild("Soccer" .. player.Name)
        character.SoccerBall.Parent = backpack
        return ServerBalls:FindFirstChild("Soccer" .. player.Name)
    end

    local Ball = ServerBalls:FindFirstChild("Soccer" .. player.Name) or GetBall()
    Ball.CanCollide = false
    Ball.Massless = true
    Ball.CustomPhysicalProperties = PhysicalProperties.new(0.0001, 0, 0)

    if target ~= player then
        local tchar = target.Character
        if tchar and tchar:FindFirstChild("HumanoidRootPart") and tchar:FindFirstChild("Humanoid") then
            local troot = tchar.HumanoidRootPart
            local thum = tchar.Humanoid

            if Ball:FindFirstChildWhichIsA("BodyVelocity") then
                Ball:FindFirstChildWhichIsA("BodyVelocity"):Destroy()
            end

            local bv = Instance.new("BodyVelocity")
            bv.Name = "FlingPower"
            bv.Velocity = Vector3.new(9e8, 9e8, 9e8)
            bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
            bv.P = 9e900
            bv.Parent = Ball

            workspace.CurrentCamera.CameraSubject = thum
            local StartTime = os.time()
            repeat
                if troot.Velocity.Magnitude > 0 then
                -- Cálculo da posição ajustada com base na velocidade do alvo
                local pos_x = troot.Position.X + (troot.Velocity.X / 1.5)
                local pos_y = troot.Position.Y + (troot.Velocity.Y / 1.5)
                local pos_z = troot.Position.Z + (troot.Velocity.Z / 1.5)

                -- Posiciona a bola diretamente na posição ajustada
                local position = Vector3.new(pos_x, pos_y, pos_z)
                Ball.CFrame = CFrame.new(position)
                Ball.Orientation += Vector3.new(45, 60, 30)
else
for i, v in pairs(tchar:GetChildren()) do
if v:IsA("BasePart") and v.CanCollide and not v.Anchored then
Ball.CFrame = v.CFrame
task.wait(1/2000)
end
end
end
                task.wait(1/6000)
            until troot.Velocity.Magnitude > 1000 or thum.Health <= 0 or not tchar:IsDescendantOf(workspace) or target.Parent ~= players
            workspace.CurrentCamera.CameraSubject = humanoid
        end
    end
end

Troll:AddButton({
    Name = "Fling Ball",
    Callback = function()
        FlingBall(game:GetService("Players")[selectedPlayerName])
    end
})

local Anti = Window:MakeTab({ Title = "Anti", Icon = "Home" })
Anti:AddToggle({
   Name = "Anti Sit",
   Default = false,
   Callback = function()
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService") -- Opcional, caso queira adicionar efeitos visuais no futuro
local LocalPlayer = Players.LocalPlayer

-- Configurações básicas
_G.AntiSit = _G.AntiSit or false -- Variável global para rastrear o estado do toggle

-- Função que aplica ou remove o Anti-Sit do personagem atual
local function aplicarAntiSit(character)
    local humanoid = character:WaitForChild("Humanoid", 5)
    if humanoid then
        if _G.AntiSit then
            humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
            if humanoid.Sit then
                humanoid.Sit = false -- Força o personagem a levantar se já estiver sentado
            end
        else
            humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false)
        end
    end
end

-- Função principal do Toggle
local function toggleAntiSit()
    _G.AntiSit = not _G.AntiSit -- Inverte o estado atual (true/false)
    
    -- Notificação visual no console/chat para você saber se ligou ou desligou
    if _G.AntiSit then
        print("[Anti-Sit]: ATIVADO")
    else
        print("[Anti-Sit]: DESATIVADO")
    end

    -- Aplica no personagem atual
    if LocalPlayer.Character then
        aplicarAntiSit(LocalPlayer.Character)
    end
end

-- Mantém o Anti-Sit funcionando mesmo se você morrer e o personagem resetar
LocalPlayer.CharacterAdded:Connect(function(character)
    -- Espera um momento curto para o humanoid carregar completamente
    task.wait(0.5)
    aplicarAntiSit(character)
end)

-- Executa o toggle (toda vez que você rodar esse bloco de código abaixo, ele liga/desliga)
toggleAntiSit()
end
})

local RunService = game:GetService("RunService")

local antiFlingActive = false
local physicsConnection = nil
local folderSignalConnection = nil

-- Tabela para gerenciar as partes da pasta que perderão a colisão
local trackedSoccerParts = {}

-- Caminho direto para a pasta alvo
local function getSoccerFolder()
    local workspaceCom = workspace:FindFirstChild("WorkspaceCom")
    if workspaceCom then
        return workspaceCom:FindFirstChild("001_SoccerBalls")
    end
    return nil
end

-- Adiciona a parte na lista de monitoramento de colisão se ela pertencer à pasta
local function checkPart(item)
    if not antiFlingActive or not item:IsA("BasePart") then return end
    
    local soccerFolder = getSoccerFolder()
    if soccerFolder and item:IsDescendantOf(soccerFolder) then
        trackedSoccerParts[item] = true
    end
end

-- Aplica CanCollide = false continuamente na fase física do motor (Garante que nada reative)
local function applyNoCollision()
    for item in pairs(trackedSoccerParts) do
        if item and item.Parent then
            item.CanCollide = false
        else
            trackedSoccerParts[item] = nil -- Limpeza automática se o objeto for deletado
        end
    end
end

-- Chamada do Toggle integrada à sua interface existente
Anti:AddToggle({
    Name = "Anti Fling Ball (Folder)",
    Default = false,
    Callback = function(Value)
        antiFlingActive = Value
        
        if antiFlingActive then
            table.clear(trackedSoccerParts)
            
            local soccerFolder = getSoccerFolder()
            if soccerFolder then
                -- 1. Capta tudo o que já está dentro da pasta no momento em que ligou
                for _, item in ipairs(soccerFolder:GetDescendants()) do
                    checkPart(item)
                end
                
                -- 2. Escuta de forma leve apenas a entrada de novos objetos na pasta
                folderSignalConnection = soccerFolder.DescendantAdded:Connect(function(item)
                    task.defer(checkPart, item)
                end)
            end
            
            -- 3. Mantém a colisão desligada sincronizado com o motor de física
            physicsConnection = RunService.Stepped:Connect(applyNoCollision)
        else
            -- Desliga o sistema limpando a memória do Roblox com total segurança
            if physicsConnection then physicsConnection:Disconnect(); physicsConnection = nil end
            if folderSignalConnection then folderSignalConnection:Disconnect(); folderSignalConnection = nil end
            table.clear(trackedSoccerParts)
        end
    end
})

game:GetService("CoreGui")["redz Library V5"].Hub.Image = "rbxassetid://114476297734336"
wait(0.5)
game:GetService("Players").LocalPlayer.Character:FindFirstChildWhichIsA("Humanoid"):SetStateEnabled(Enum.HumanoidStateType.Seated, true)
